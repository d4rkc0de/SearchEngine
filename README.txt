- To build the project and generate a jar file run:
    mvn clean test install
- To run the jar file:
    After build access target directory and run the following command:
    java -jar SearchEngine-1.0-SNAPSHOT.jar /home/user/yourPath